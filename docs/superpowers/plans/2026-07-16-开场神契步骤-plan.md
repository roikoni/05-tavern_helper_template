# 开场捏角新增「神契」步骤 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 src/开场 捏角流程的行装功法(4)和属性(5)之间插入「神契」步骤，允许玩家从六位古神中择一作为开局陪伴者。

**Architecture:** 新建 catalog 数据文件 + 新建 Vue 组件，修改 App.vue/StepIndicator/StepConfirm/draft/save 五个现有文件，总步数从 7 扩至 8，后续步骤号顺延。

**Tech Stack:** Vue 3 + Pinia + SCSS + TypeScript，复用项目 theme.scss 的 xianxia-card/xianxia-btn 混入，头像复用 avatarStore.ts。

## Global Constraints

- 使用 TypeScript 而非 JavaScript
- 优先使用 Vue 组件 + `<style scoped lang="scss">` 书写样式
- 使用 `@use './theme' as *;` 导入主题混入
- 遵循现有文件命名规范（中文文件名）
- 卡片网格与 StepShop 风格一致
- 代码变更后酒馆网页热重载，无需手动 build

---

## File Map

| 文件 | 职责 | 操作 |
|------|------|------|
| `src/开场/catalog/神契.ts` | 六古神静态数据 | 新建 |
| `src/开场/components/StepDivineContract.vue` | 神契选择卡片网格 UI | 新建 |
| `src/开场/draft.ts` | 捏角草稿（已选神契） | 修改 |
| `src/开场/components/StepIndicator.vue` | 步骤标题列表 | 修改 |
| `src/开场/App.vue` | 步骤路由/总数/重置 | 修改 |
| `src/开场/components/StepConfirm.vue` | 总览展示神契 | 修改 |
| `src/开场/lib/save.ts` | 存档类型定义 | 修改 |

---

### Task 1: 新建神契数据目录 `src/开场/catalog/神契.ts`

**Files:**
- Create: `src/开场/catalog/神契.ts`

**Interfaces:**
- Produces: `神契条目` interface, `神契列表: 神契条目[]`

- [ ] **Step 1: 创建神契数据文件**

```typescript
export interface 神契条目 {
  名称: string;
  身份: string;
  位阶: string;
  领域: string;
  神契技能: string;
  神契描述: string;
  描述: string;
}

export const 神契列表: 神契条目[] = [
  {
    名称: '莫尔迦',
    身份: '欲望之母，十一古神之首',
    位阶: '上位神',
    领域: '欲望·秩序',
    神契技能: '至高精神场域',
    神契描述: '建立精神力场庇护同伴，安抚心神、驱散旧日污染，可共享神识感知',
    描述: '天渊绿洲领主，以严厉手腕守护旧日封印',
  },
  {
    名称: '维拉',
    身份: '欢宴之主',
    位阶: '上位神',
    领域: '欢宴·共感',
    神契技能: '欢宴幻境',
    神契描述: '构筑集体幻境将宴席化为临时避难所，安抚惊惧、调谐情绪、驱散噩梦',
    描述: '游走世间宴席的巡灯人，将善意伪装成顺手寻乐',
  },
  {
    名称: '斯芬克丝',
    身份: '寂静之唇',
    位阶: '上位神',
    领域: '寂静·共情',
    神契技能: '寂静心域',
    神契描述: '展开寂静心域平复心绪，感知情绪波动，安抚精神崩溃，调制安神香',
    描述: '南岭深山隐居者，怯生寡言却愿默默修补他人心神',
  },
  {
    名称: '拉维妮亚',
    身份: '痴情之锁',
    位阶: '上位神',
    领域: '痴情·誓约',
    神契技能: '护誓红线',
    神契描述: '以红线锚定心魂，斩断邪契与精神操控，辨识真心与欺骗',
    描述: '东荒巡行者，斩断一切以爱为名的枷锁',
  },
  {
    名称: '克茜拉',
    身份: '深渊的低语',
    位阶: '上位神',
    领域: '深渊·净海',
    神契技能: '净海低鸣',
    神契描述: '分辨旧日低语与真正求救声，隔离精神污染，以潮灯安抚亡者残魂',
    描述: '北冥海岸的孤独守望者，将污染压回海中',
  },
  {
    名称: '胧',
    身份: '戏言之蝶，最弱古神',
    位阶: '下位神',
    领域: '戏言·蝶影',
    神契技能: '戏言·蝶影步',
    神契描述: '以戏言扰乱敌人感知，蝶影步短距离闪避与藏匿，可识破谎言',
    描述: '流落街头的轻快少女，嘴上不饶人，心底渴望被选择',
  },
];
```

- [ ] **Step 2: 验证文件编译**

Run: 检查 IDE 是否有类型错误（新建文件无依赖，应无错误）

- [ ] **Step 3: Commit**

```bash
git add src/开场/catalog/神契.ts
git commit -m "feat(开场): 新增神契数据目录，定义六古神条目

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: draft.ts 新增已选神契字段

**Files:**
- Modify: `src/开场/draft.ts`

**Interfaces:**
- Produces: `draftStore.已选神契: Ref<string>`, 在 `重置()` 中清空

- [ ] **Step 1: 在 draft.ts 新增已选神契**

将 `src/开场/draft.ts` 中的内容改为：

```typescript
import { defineStore } from 'pinia';
import { ref } from 'vue';

/**
 * 捏角阶段的临时草稿，不绑定 mvu 变量。
 * 沙盒模式：全功能开放，无限制。
 * - 已选功法：玩家选定的功法名，提交时通过 prompt 交给 AI 由其 insert 到 /初始功法/
 * - 已选神契：玩家选定的古神名，空字符串表示未选择
 */
export const useDraftStore = defineStore('开场.draft', () => {
  const 已选功法 = ref<string[]>([]);
  const 已选神契 = ref<string>('');
  const 开局身份 = ref<string>('');
  const 本源基调 = ref<'正经' | '搞笑' | '涩涩'>('正经');

  function 切换功法(名: string) {
    const i = 已选功法.value.indexOf(名);
    if (i >= 0) 已选功法.value.splice(i, 1);
    else 已选功法.value.push(名);
  }

  function 选神契(名: string) {
    if (已选神契.value === 名) {
      已选神契.value = '';
    } else {
      已选神契.value = 名;
    }
  }

  function 清空() {
    已选功法.value = [];
  }

  function 重置() {
    已选功法.value = [];
    已选神契.value = '';
    开局身份.value = '';
    本源基调.value = '正经';
  }

  return {
    已选功法, 已选神契, 开局身份, 本源基调,
    切换功法, 选神契, 清空, 重置,
  };
});
```

- [ ] **Step 2: Commit**

```bash
git add src/开场/draft.ts
git commit -m "feat(开场): draft 新增已选神契字段与选神契方法

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: StepIndicator.vue 插入「神契」标题

**Files:**
- Modify: `src/开场/components/StepIndicator.vue:25`

**Interfaces:**
- Consumes: 无新依赖
- Produces: 步骤标题数组变为 8 项

- [ ] **Step 1: 修改步骤标题数组**

将 [StepIndicator.vue:25](src/开场/components/StepIndicator.vue#L25) 的：

```typescript
const 步骤标题 = ['出身', '道途', '宗门', '行装功法', '属性', '出生点', '总览'];
```

改为：

```typescript
const 步骤标题 = ['出身', '道途', '宗门', '行装功法', '神契', '属性', '出生点', '总览'];
```

- [ ] **Step 2: Commit**

```bash
git add src/开场/components/StepIndicator.vue
git commit -m "feat(开场): 步骤指示器插入「神契」标题

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 新建神契选择组件 StepDivineContract.vue

**Files:**
- Create: `src/开场/components/StepDivineContract.vue`

**Interfaces:**
- Consumes: `useDataStore` from `../store`, `useDraftStore` from `../draft`, `神契列表` from `../catalog/神契`, `avatarOf` from `../../修仙状态栏/avatarStore`
- Produces: 选中古神 → `draft.已选神契` + `data.主角.神契装备`

- [ ] **Step 1: 创建组件文件**

```vue
<template>
  <section class="step">
    <h2>神契 <span class="optional">（可选）</span></h2>
    <p class="subtitle">选择一位古神缔结神契，作为开局陪伴者</p>

    <div class="grid">
      <button
        v-for="神 in 神契列表"
        :key="神.名称"
        class="card"
        :class="{ active: 已选神契 === 神.名称 }"
        @click="选神契(神.名称)"
      >
        <div class="avatar-wrap">
          <img :src="avatarOf(神.名称)" :alt="神.名称" class="avatar" />
          <span v-if="已选神契 === 神.名称" class="check-mark">✓</span>
        </div>
        <div class="name">{{ 神.名称 }}</div>
        <div class="identity">
          <span class="badge divine">神</span>
          {{ 神.身份 }}
        </div>
        <div class="tier-domain">
          <span class="tier">{{ 神.位阶 }}</span>
          <span class="sep">·</span>
          <span class="domain">{{ 神.领域 }}</span>
        </div>
        <div class="skill-name">{{ 神.神契技能 }}</div>
        <div class="skill-desc">{{ 神.神契描述 }}</div>
      </button>
    </div>

    <p class="hint">神契可选，不选亦可开局。所选古神将在开场剧情中以陪伴者身份自然登场。</p>
  </section>
</template>

<script setup lang="ts">
import { storeToRefs } from 'pinia';
import { useDataStore } from '../store';
import { useDraftStore } from '../draft';
import { 神契列表 } from '../catalog/神契';
import { avatarOf } from '../../修仙状态栏/avatarStore';

const { data } = storeToRefs(useDataStore());
const draft = useDraftStore();
const { 已选神契 } = storeToRefs(draft);

function 选神契(名: string) {
  draft.选神契(名);
  _.set(data.value, '主角.神契装备', draft.已选神契);
}
</script>

<style scoped lang="scss">
@use './theme' as *;

.step {
  padding: 1.4rem 1.4rem 1.8rem;
  color: $paper-cold;
  font-size: 1.05rem;
  animation: step-in 0.4s ease-out;
  @include mobile { padding: 0.8rem 0.7rem 1.2rem; font-size: 0.92rem; }
  @include tablet { padding: 1rem 1rem 1.4rem; }
}
@keyframes step-in {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

h2 {
  @include gold-heading;
  font-size: 1.7rem;
  margin: 0 0 0.3rem;
  @include mobile { font-size: 1.3rem; }
  .optional {
    font-size: 0.6em;
    font-weight: 400;
    color: $paper-dim;
    letter-spacing: 0.05em;
  }
}

.subtitle {
  font-size: 0.95rem;
  font-weight: 500;
  color: $paper-soft;
  margin: 0.2rem 0 1.2rem;
  font-style: italic;
  letter-spacing: 0.08em;
  @include mobile { font-size: 0.82rem; margin-bottom: 0.8rem; }
}

.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.8rem;
  @include tablet { grid-template-columns: repeat(2, 1fr); }
  @include mobile { grid-template-columns: 1fr; gap: 0.6rem; }
}

.card {
  @include xianxia-card;
  text-align: center;
  padding: 1rem 0.8rem 0.9rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.35rem;
  cursor: pointer;
  color: $paper-cold;
  font-size: 1rem;
  @include mobile { padding: 0.7rem 0.6rem; font-size: 0.9rem; }

  .avatar-wrap {
    position: relative;
    width: 80px;
    height: 80px;
    border-radius: $r-md;
    overflow: hidden;
    border: 2px solid rgba(200,200,210,0.15);
    margin-bottom: 0.3rem;
    flex-shrink: 0;
    @include mobile { width: 64px; height: 64px; }

    .avatar {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .check-mark {
      position: absolute;
      top: 4px;
      left: 4px;
      width: 24px;
      height: 24px;
      border-radius: 50%;
      background: $blood-glow;
      color: #f0e8e0;
      font-size: 0.8rem;
      font-weight: bold;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 0 8px rgba(216,56,56,0.5);
      @include mobile { width: 20px; height: 20px; font-size: 0.7rem; }
    }
  }

  .name {
    font-family: $font-serif;
    font-weight: bold;
    font-size: 1.2rem;
    color: $paper-cold;
    letter-spacing: 0.1em;
    @include mobile { font-size: 1.05rem; }
  }

  .identity {
    font-size: 0.82rem;
    font-weight: 500;
    color: $paper-soft;
    display: flex;
    align-items: center;
    gap: 0.3em;
    @include mobile { font-size: 0.75rem; }

    .badge {
      font-size: 0.75em;
      color: #ffd8a8;
      border: 1px solid rgba(212,154,90,0.6);
      padding: 0.1em 0.45em;
      border-radius: $r-pill;
      background: rgba(80,50,20,0.35);
      text-shadow: 0 0 6px rgba(212,154,90,0.5);
      &.divine {
        letter-spacing: 0.1em;
      }
    }
  }

  .tier-domain {
    font-size: 0.78rem;
    font-weight: 500;
    color: $paper-dim;
    @include mobile { font-size: 0.72rem; }
    .sep { margin: 0 0.2em; opacity: 0.5; }
  }

  .skill-name {
    font-size: 0.9rem;
    font-weight: 600;
    color: $blood-glow;
    margin-top: 0.25rem;
    text-shadow: 0 0 6px rgba(168,51,51,0.3);
    letter-spacing: 0.05em;
    @include mobile { font-size: 0.82rem; }
  }

  .skill-desc {
    font-size: 0.8rem;
    font-weight: 500;
    color: $paper-dim;
    line-height: 1.55;
    @include mobile { font-size: 0.74rem; }
  }
}

.hint {
  font-size: 0.92rem;
  font-weight: 500;
  color: $paper-soft;
  margin-top: 1.3rem;
  font-style: italic;
  text-align: center;
  letter-spacing: 0.08em;
  padding: 0.6rem;
  border-top: 1px dashed rgba(200,200,210,0.12);
  @include mobile { font-size: 0.8rem; padding: 0.45rem; margin-top: 0.9rem; }
}
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/开场/components/StepDivineContract.vue
git commit -m "feat(开场): 新增神契选择卡片网格组件

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: App.vue 流程调整

**Files:**
- Modify: `src/开场/App.vue`

**Interfaces:**
- Consumes: `StepDivineContract` from `./components/StepDivineContract.vue`
- Produces: 步骤 5→神契，原 5/6/7 顺延为 6/7/8; 重置含神契清理; 存档带神契

- [ ] **Step 1: 修改 App.vue**

改动汇总（按行号对应原文件）：

**(a) 导入新组件** — 在 `<script setup>` 的 import 区域新增一行：

```typescript
import StepDivineContract from './components/StepDivineContract.vue';
```

**(b) 总步数** — 将 `const 总步数 = 7;` 改为：

```typescript
const 总步数 = 8;
```

**(c) 步骤路由** — 在 `<template>` 的 `<StepShop v-else-if="当前步 === 4" key="4" />` 之后插入神契步骤，并将后续步骤号顺延。将：

```html
<StepShop        v-else-if="当前步 === 4" key="4" />
<StepAttribute   v-else-if="当前步 === 5" key="5" />
<StepBirthplace  v-else-if="当前步 === 6" key="6" />
<StepConfirm    v-else-if="当前步 === 7" key="7" />
```

改为：

```html
<StepShop          v-else-if="当前步 === 4" key="4" />
<StepDivineContract v-else-if="当前步 === 5" key="5" />
<StepAttribute     v-else-if="当前步 === 6" key="6" />
<StepBirthplace    v-else-if="当前步 === 7" key="7" />
<StepConfirm      v-else-if="当前步 === 8" key="8" />
```

**(d) doSave — 存档带神契** — 在 `doSave` 的 `saveCreator({...})` 对象中新增一行：

```typescript
已选神契: draftStore.已选神契,
```

插入在 `已选功法: klona(draftStore.已选功法),` 之后。

**(e) doLoad — 读档恢复神契** — 在 `doLoad` 中，`draftStore.已选功法 = klona(s.已选功法);` 之后新增：

```typescript
if (typeof s.已选神契 === 'string') draftStore.已选神契 = s.已选神契;
```

同时也需要恢复 `data.主角.神契装备`，在该行后追加：

```typescript
if (typeof s.已选神契 === 'string') _.set(data.value, '主角.神契装备', s.已选神契);
```

**(f) doReset — 重置清理** — 在 `doReset` 中，`draftStore.重置();` 已含 `已选神契 = ''` 清理，无需额外改动。`data.value.主角.神契装备` 已在 reset 对象中设为 `''`，确认无误。

**(g) reset 警告文案** — 将 reset 弹窗中的描述文字更新，`清空所有已选选项（姓名、种族、境界、宗门、流派、灵根、装备、功法、六维、灵石）` 改为：

```html
清空所有已选选项（姓名、种族、境界、宗门、流派、灵根、装备、功法、神契、六维、灵石）。此操作不可撤销。
```

- [ ] **Step 2: Commit**

```bash
git add src/开场/App.vue
git commit -m "feat(开场): 插入神契步骤，总步数 7→8，存档支持神契

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: StepConfirm.vue 总览展示神契

**Files:**
- Modify: `src/开场/components/StepConfirm.vue`

**Interfaces:**
- Consumes: `useDraftStore` 的 `已选神契`, `神契列表`
- Produces: 总览中显示已选神契与技能

- [ ] **Step 1: 在总览中加入神契展示**

**(a) 导入神契列表** — 在 `<script setup>` 中，`import { useDraftStore } from '../draft';` 之后新增：

```typescript
import { 神契列表 } from '../catalog/神契';
```

**(b) 新增计算属性** — 在 `const 已学功法 = computed(...)` 之后新增：

```typescript
const 已选神契条目 = computed(() => 神契列表.find(s => s.名称 === draft.已选神契));
```

**(c) 在模板中展示** — 在「功法」区块之后（`</ul>` 之后的 `<p v-else>` 之后），「校验错误」之前插入神契展示行：

```html
<h3>神契</h3>
<p v-if="已选神契条目">
  {{ 已选神契条目.名称 }}（{{ 已选神契条目.身份 }}）— 神契技能：{{ 已选神契条目.神契技能 }}
</p>
<p v-else class="hint">（未选择神契）</p>
```

- [ ] **Step 2: Commit**

```bash
git add src/开场/components/StepConfirm.vue
git commit -m "feat(开场): 总览展示已选神契与技能

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: save.ts 存档类型新增已选神契

**Files:**
- Modify: `src/开场/lib/save.ts`

**Interfaces:**
- Produces: `CreatorSave.已选神契?: string`

- [ ] **Step 1: 在 CreatorSave 接口新增可选字段**

在 `src/开场/lib/save.ts` 的 `CreatorSave` 接口中，`已选功法: string[];` 之后新增：

```typescript
/** 已选神契古神名（空字符串或 undefined 表示未选择） */
已选神契?: string;
```

- [ ] **Step 2: Commit**

```bash
git add src/开场/lib/save.ts
git commit -m "feat(开场): 存档类型新增已选神契字段

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Verification

全部任务完成后，验证以下场景：

1. 捏角流程从步骤 1 走到步骤 8：步骤指示器显示 8 步，神契在第 5 步
2. 在神契步骤中，6 张卡片正确显示头像、名称、身份、位阶、领域、技能
3. 点击卡片选中，再点取消；换选另一古神
4. 「上一步」「下一步」导航正确：从行装功法→神契→属性
5. 总览中显示已选神契信息；未选时显示"未选择神契"
6. 存档后再载入，神契选择保留
7. 重置后神契选择被清空
8. 打开酒馆网页，检查热重载是否生效，DOM 是否正常渲染
